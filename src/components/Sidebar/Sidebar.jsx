import React from 'react'

// const pageQuery = graphql`

// `

// should pass in headings from article
const Sidebar = (props) => {
	// const data = useStaticQuery(pageQuery)
	return (
	<aside>
		this is a sidebar
	</aside>
)
	}



export default Sidebar